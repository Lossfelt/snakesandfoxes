name: Tester

on:
  push:
  pull_request:

jobs:
  tester:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Oppsett Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Kjør tester
        run: node --test tests/
